# Skip TLS verification for outbound LLM API calls only (local dev with broken/missing CA certs).
LLM_TLS_INSECURE=1

# Alternatively, point Node at your corporate/root CA bundle instead of disabling verification:
# NODE_EXTRA_CA_CERTS=/path/to/ca-bundle.pem
