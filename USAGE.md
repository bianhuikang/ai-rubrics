# 使用说明

## 必须步骤

1. 安装依赖：

```bash
npm install
```

2. 启动应用：

```bash
npm run dev
```

3. 打开页面：

```text
http://localhost:3000
```

4. 新建任务，填写：

- 任务 ID
- Prompt
- 产物 URL 列表
- Rubrics，可不填；不填会自动生成

5. 点击“创建并执行”。

6. 在结果区点击每个 URL 后面的“手动检查”。

7. 逐条判断 rubrics：

- 符合：点击“符合”
- 不符合：点击“不符合”，填写原因

8. 回到主界面复制：

- Rubrics
- 打分数组
- 不符合原因汇总

## 说明

- 首次启动会自动创建数据库。
- 默认已写入模型配置。
- 默认使用手动检查模式。
